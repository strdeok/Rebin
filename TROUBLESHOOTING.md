# Re-bin 트러블슈팅

[← README로 돌아가기](./readme.md)

## 1. 배포 환경에서 Google 로그인 팝업이 안정적으로 동작하지 않음

### 문제

Firebase Google 로그인에 팝업 방식을 사용했을 때 배포 도메인·브라우저 환경에 따라 인증 창이 차단되거나, 인증 후 앱 상태로 자연스럽게 돌아오지 않는 문제가 있었습니다.

### 해결

로그인 흐름을 팝업 방식에서 **리디렉션 방식**으로 전환하고, Firebase 인증 도메인 설정을 배포 도메인과 일치시켰습니다. 인증 완료 후에는 Firebase의 인증 상태 변화를 구독해 관심 장소와 현재 위치 초기화를 이어서 수행하도록 구성했습니다.

### 결과

브라우저의 팝업 정책에 덜 의존하는 인증 흐름을 만들었고, 인증 완료 뒤 필요한 사용자 데이터만 조회하도록 흐름을 정리했습니다.

---

## 2. 지도·현재 위치·선택 수거함 상태가 화면마다 엇갈림

### 문제

지도 화면은 현재 위치, 선택한 수거함, 지도 중심 이동, 경로 표시, 관심 장소처럼 서로 영향을 주는 상태가 많습니다. 화면별 로컬 상태로 관리하면 목록에서 선택한 장소가 지도에 반영되지 않거나, 로그인 뒤 관심 장소가 갱신되지 않는 문제가 발생하기 쉬웠습니다.

### 해결

공유가 필요한 지도 상태를 Zustand 스토어로 모았습니다. 수거함 선택 시 선택 장소·지도 중심·경로 표시 상태를 함께 갱신하고, 인증 상태가 바뀌면 관심 장소와 위치 정보를 스토어에 반영하도록 구성했습니다.

### 결과

목록, 지도, 상세 패널이 같은 상태를 기준으로 렌더링되어 사용자 행동에 따른 화면 전환을 예측 가능하게 만들었습니다.

---

## 3. 초기 진입 시 지도 외 화면 코드까지 함께 로드됨

### 문제

온보딩, 로그인, 지도, 마이페이지, 챗봇은 한 번에 필요하지 않지만 단일 번들에 포함되면 첫 진입 비용이 커집니다.

### 해결

각 페이지를 `React.lazy`로 분리하고 `Suspense`의 공통 로딩 화면을 적용했습니다. 사용자가 실제로 이동한 경로의 화면 코드만 요청하도록 구성했습니다.

### 결과

초기 로딩 경로를 가볍게 유지하면서도, 화면 전환 중에는 일관된 로딩 상태를 제공할 수 있게 됐습니다.
