# Re-bin 트러블슈팅

[← README로 돌아가기](./readme.md)

## 1. 지도에 모든 마커를 렌더링해 탐색 성능이 저하됨

### 문제

지도 이동·확대·축소마다 전체 수거함 마커를 렌더링하면 화면 밖 마커까지 계산하고 DOM에 반영하게 됩니다. 특히 낮은 zoom에서는 마커 수가 많아 평균 렌더링 시간이 15.4ms까지 증가했습니다.

### 해결

지도 bounds와 zoom을 기준으로 현재 화면 밖 마커를 제외했습니다. 낮은 zoom에서는 `MarkerClusterer`로 마커를 묶고, 높은 zoom에서는 현재 영역의 개별 마커만 렌더링하도록 분기했습니다.

### 결과

평균 렌더링 시간을 **15.4ms → 약 1ms**로 줄여 지도 조작 시 화면 반응성을 개선했습니다.

---

## 2. 지도 상태 변경이 관련 없는 컴포넌트까지 리렌더링함

### 문제

현재 위치, 선택 수거함, 지도 중심, 경로 표시, 관심 장소처럼 지도 화면의 상태는 서로 연결돼 있습니다. 전역 상태를 넓게 구독하면 지도 이동처럼 빈번한 상태 변경이 관련 없는 컴포넌트까지 다시 렌더링하는 문제가 생깁니다.

### 해결

공유가 필요한 지도 상태를 Zustand 스토어로 모으고, 각 컴포넌트에서는 selector와 `useShallow`를 사용해 필요한 상태와 액션만 구독하도록 변경했습니다.

### 결과

상태의 일관성을 유지하면서도 구독 범위를 줄여 지도 이동·확대·축소 과정의 불필요한 리렌더링을 줄였습니다.

---

## 3. 초기 진입 비용이 커 모바일 성능 점수가 낮음

### 문제

초기 진입에서 즉시 필요하지 않은 화면 코드와 애니메이션·데이터 SDK가 함께 로드되면서 메인 번들이 581KB였고, 모바일 Lighthouse 점수는 66점이었습니다.

### 해결

Firestore Lite를 적용하고, `LazyMotion`과 `React.lazy`·`Suspense`로 애니메이션과 라우트 코드를 분리했습니다. LCP 이미지는 우선 로딩하도록 구성했습니다.

### 결과

메인 번들을 **581KB → 475KB**로 줄이고, 모바일 Lighthouse를 **66점 → 94점**으로 개선했습니다.
